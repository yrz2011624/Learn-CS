After editing the file, the changes reside in your working directory.

Run `git add filename` to move the changes to the staging area.

Run `git commit -m "message"` to package everything in the staging area into a commit and store it in your local repository.

Run `git push` to upload that commit from your local repository to the remote repository on GitHub
